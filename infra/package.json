{
  "name": "infra",
  "version": "0.1.0",
  "private": true,
  "description": "Self-hosted AWS security and configuration checker built with AWS CDK and Lambda.",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "npm run test:cdk && npm run test:lambda",
    "test:cdk": "jest",
    "test:lambda": "python3 -m compileall -q lambda/checker test/lambda && PYTHONPATH=lambda/checker python3 -m unittest discover -s test/lambda -p 'test_*.py'",
    "cdk": "cdk"
  },
  "devDependencies": {
    "@types/jest": "^30",
    "@types/node": "^24.10.1",
    "jest": "^30",
    "ts-jest": "^29",
    "aws-cdk": "2.1134.0",
    "ts-node": "^10.9.2",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.263.0",
    "constructs": "^10.5.0"
  }
}
